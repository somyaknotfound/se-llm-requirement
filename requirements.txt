# Python 3.11+ (developed and tested on 3.12.5, Windows 11)
# No hosted LLM APIs anywhere in this list — inference is local via Ollama.

# --- LLM transport -------------------------------------------------------
requests>=2.32.0          # Ollama REST client (/api/generate, /api/tags)

# --- Retrieval -----------------------------------------------------------
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# --- Corpus parsing ------------------------------------------------------
pypdf>=4.3.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
tiktoken>=0.7.0           # proxy tokenizer for reproducible chunk sizing

# --- Data / reporting ----------------------------------------------------
pandas>=2.2.0
matplotlib>=3.9.0
PyYAML>=6.0.1
