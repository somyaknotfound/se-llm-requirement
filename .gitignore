.venv/
__pycache__/
*.pyc
.env

# Embedding index and cached vectors are derived artifacts — rebuild with
# `python -m src.index build`. Everything else under corpus/ IS committed:
# raw sources and chunks are the evidence base for traceability claims.
corpus/index/

# Figures are regenerated by report.py, but keep them committed so the report
# renders from a clean checkout. Uncomment if the repo gets heavy.
# figures/
